// React
import React from 'react';

// Styles and icons
import './Example.css';

const Example = ({ isDarkMode }) => {
    return (
        <div className="example">
            <h1>Example Section</h1>
            <p>
                Example text for an example description.
                Example text for an example description.
                Example text for an example description.
                Example text for an example description.
                Example text for an example description.
            </p>
        </div>
    );
};

export default Example;
